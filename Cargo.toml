[package]
name = "repobox"
version = "0.1.0"
edition = "2024"
description = "Git permission layer for AI agents"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"

[dev-dependencies]
